package gameDemo.Abstract;

import gameDemo.Entities.Campaign;

public interface CampaignService {
    	void add(Campaign campaign);
    	void upddate(Campaign campaign);
    	void delete(Campaign campaign);
}
